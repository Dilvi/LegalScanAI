enable_spacy_fallback: false

patterns:
  # 🧍 ФИО
  - type: PER
    regex:
      - '\b[А-ЯЁ][а-яё]+ [А-ЯЁ][а-яё]+ [А-ЯЁ][а-яё]+\b'   # Фамилия Имя Отчество
      - '\b[А-ЯЁ][а-яё]+ [А-ЯЁ][а-яё]+\b'               # Фамилия Имя (на всякий случай)

  # 🏙️ Адреса
  - type: ADDRESS
    regex:
      - 'г\. ?[А-ЯЁ][а-яё]+(?:, [^,\n]+)*'
      - 'ул\. ?[А-ЯЁ][а-яё]+(?:, [^,\n]+)*'
      - 'проспект [А-ЯЁ][а-яё]+'
      - 'д\.\s?\d+[А-Яа-яA-Za-z]*'
      - 'кв\.\s?\d+'
      - '(?i)\bСвердловской области\b'    # регион
      - '(?i)\bленинском районе\b'
      - '(?i)\bкировском районе\b'

  # 🆔 Паспортные данные
  - type: PASSPORT
    regex:
      - 'паспорт серии \d{4} № \d+'

  # 📅 Даты
  - type: DATE
    regex:
      - '\b\d{2}\.\d{2}\.\d{4}\b'                     # 02.05.1964
      - '\b\d{1,2}\s+[А-Яа-яё]+\s+\d{4}\b'            # 14 января 2024
      - '"\s?\d{1,2}\s?"\s?\d{1,2}\s?\d{4}'           # " 23 " 01 2004

  # 💰 Деньги
  - type: MONEY
    regex:
      - '\b\d[\d\s]{2,}\s?р\.?'

  # 🚘 VIN
  - type: VIN
    regex:
      - '\b[A-HJ-NPR-Z0-9]{17}\b'

  # 🚘 Госномер
  - type: REGNUMBER
    regex:
      - '\b[АВЕКМНОРСТУХ]\s?\d{3}\s?[АВЕКМНОРСТУХ]{2}\s?\d{2,3}\b'
